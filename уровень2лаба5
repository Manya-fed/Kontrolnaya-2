#19. В заданной матрице удалить все строки, содержащие нулевыеэлементы. Удаление строки оформить в виде метода.\

def r(matrix):
    return [row for row in matrix if 0 not in row]


A = [
    [5, 2, 0, 4],
    [1, 8, 9, 10],
    [3, 0, 15, 16],
    [4, 20, 21, 22],
    [2, 26, 27, 28]
]

print("Исходная матрица:")
for row in A:
    print(row)

A_new = r(A)

print("\nМатрица после удаления строк с нулевыми элементами:")
for row in A_new:
    print(row)
